#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(){
    int v;
    while (scanf("%d", &v) != EOF)
    {
        /* code */
        scanf("%d", &v);
        printf("%d\n", v);
    }
    
    
}